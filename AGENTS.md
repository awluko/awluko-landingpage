# Repo Agent Rules

## Scope
- Applies to `/Users/andrewlukosevic/Documents/Programming/awluko-landingpage`.

## Project Type
- Astro + Tailwind personal site.

## Tooling
- Package manager: `npm`.
- Scripts:
  - `npm run dev`
  - `npm run build`
  - `npm run preview`
  - `npm run astro`

## Definition of Done
For code/content changes, run:
1. `npm run build`

Optional local verification:
- `npm run dev` (or `npm run preview`) and validate changed page.

## Working Rules
- Preserve existing Astro structure and styling approach.
- Keep content and layout changes scoped to request.

## Safety
- No destructive commands unless explicitly requested.

## Handoff
- Summarize changed files, user-visible impact, and verification evidence.
